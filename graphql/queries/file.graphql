query File($id: String!) {
    file(id: $id) {
        errors {
            ...FileError
        }
        data {
            id
            createdAt
            updatedAt
            title
            text
        }
    }
}
